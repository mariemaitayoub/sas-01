const prompt = require('prompt-sync')();

let a=Number(prompt('saisis a :'));
let b=Number(prompt('saisis b :'));
 
console.log(`a + b = ${a + b}`);
console.log(`a - b = ${a - b}`);
console.log(`a * b = ${a * b}`);
console.log(`a / b = ${a / b}`);